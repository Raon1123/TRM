# 2026-07-29 — Daily Rollup

> 위임형 3h 모니터링 세션 (오케스트레이터=게시+독립감사, 기계적 tick=queue-monitor Sonnet 서브에이전트).
> 상세 per-tick 정본은 `lab/monitoring/snapshots.log`, 완주 기록은 `EXP-010 §4`, 상태 정본은
> `lab/monitoring/ACTIVE_COHORT.md`(4차 개정). 이 로그는 rollup만.

## 마일스톤 — 로컬 fig1 그리드 training-complete (56/56)

00:54Z tick에서 마지막 셀 `0221 fig1_mlp_noz_iter_k10_s1`이 step 244100 완주 → 큐 **55 done / 1 processing**.
남은 1 processing은 `0217 fig1_mlp_noz_iter_k8_s1` 하나뿐 — **알려진 claim-mv 부기버그**
(`queue_run.sh:104-118` stale-claim 오판; on-disk `.job.gpu5` vs 워커의 captured `$claimed=.gpu4`(소실)).
훈련 자체는 2026-07-28T15:10Z에 이미 terminal(step_244100 체크포인트+50 eval 온디스크), 데이터 손실 없음 —
`mv …0217_…gpu5 done/` 한 줄이면 부기 정합(cosmetic, figure 파이프라인은 `quarantine-unless-unique`로 이미 배제). **사용자 승인 대기**(강제 아님).

- **추적 run 궤적 종결**: `tf_z_iter_k6=0.982`(붕괴→재도달), `tf_z_iter_k7=1.0`(overshoot→붕괴→회복)
  둘 다 **최종 held**. tf tier 28/28 terminal — verdict-bearing 부분 100% 완전.
- 이 stall은 이번 캠페인에서 **오케스트레이터가 사전 예측→적중**한 건(12:48Z 예측 → 15:10Z 착지).

## 관찰 대상 재-scope 흡수 (브리프 4차 개정, 07-28)

주 관찰 대상이 로컬 fig1 + 10.0.12.93 ablation(둘 다 **완료 이력화**)에서 **`unistml7`**(Mission A,
H-023 인과 폐쇄 배터리)로 이전. 크론 프롬프트는 아직 fig1/10.0.12.93 문구라 divergence 있으나,
서브에이전트가 매 tick 브리프 선독으로 흡수 중(기능 이상 없음, 크론 갱신은 사용자 결정 대기).

- **unistml7** (4x RTX 3090): 하루 동안 `done 29→30`, `jobs_pending 39→38`, GPU 0-3 99-100% 지속 →
  진행 확인(카운트 단발 정체는 GPU 활성이라 2-tick 규칙 미충족, escalate 아님). leg A1(24) 통과·A2 in-flight
  (done=30<51), ⏸PI 게이트 실통과 여부는 원격 가시성 0이라 unresolved. heartbeat 손상(이중 JSON,
  raw-grep)·per-run metric-blind은 알려진 정상.
- **10.0.12.93** ablation: 52/52 완료(연속 7회 확인). `results-return/…/wandb/` 미반환.
- **imil0**: NVML driver/library mismatch로 done=0(~11일째), 신규 미보고 이슈로 브리프 기록됨.

## 사용자 조치 대기 (blocked)

세 원격 호스트 앞 결과-반환/진단 요청 3건(`2026-07-28_results-return_10.0.12.93.md` /
`…interim-results-and-heartbeat-fix_unistml7.md` / `…nvml-status-check_imil0.md`)이 초안+PI 승인됐으나
이 세션의 `/mnt/ayp/trm/instructions/` **쓰기가 권한 classifier에 거부**(Bash·Write 양쪽) — 사용자 직접 배치
또는 권한 규칙 필요. 매 tick `ls` 확인 중, 아직 미착지(기존 5개 파일만). 착지 전 원격 침묵은 정상.

## 세션 산출물 (7 파일, 서브에이전트 기록)

`snapshots.jsonl` · `evals.json`(56 runs/2800 rows) · `trm-sigmak-live.html` · `trm-rs-pipeline.html` ·
`snapshots.log` · `EXP-010 §4`(0219·0221 완주 관측) + NAS 미러(`/mnt/ayp/trm/lab/monitoring/`).
라이브/파이프라인 대시보드는 매 tick 안정 URL로 재게시(📡 4283066d… / 🎞️ 35374dde…).

## PFO 요약 — PERF-001/002 최적화

PERF-001 계측 하네스와 flame chart 도구를 PR #1로 main에 merge(`98e6c64`). 이후
`perf-002-profiling-plan_claude`에 4커밋 push(PR 미생성).

핵심 결과는 **음성**이다. flame chart와 py-spy가 `sparse_embedding.py:121`의 `unique()`를
wall time 37.6%로 지목했고, 제거 후보 P1-A는 CPU bit-exact 동등성을 통과했으나 실측 이득이
0이었다(2.41/2.39 → 2.37/2.38 it/s). py-spy로 세 상태를 관측하니 `unique`는 0.00%로 사라졌지만
대기가 다음 sync로 옮겨갔을 뿐 wall time은 불변 — **스텝이 GPU-bound**이므로 per-update sync를
하나만 제거하는 후보군 전체가 무의미하다. 다음 목표는 실제 GPU 작업량(metrics `torch.stack`,
CUDA 13.4%).

잠복 결함 3건 적발: 사전등록 M3 명령이 dual trace export 충돌로 실행 불가였던 것, flame chart가
루트 CSS `height:auto`로 백지 렌더되던 것, py-spy 빈-스택 라인 거부로 분모가 17% 줄던 것.
모두 산출물을 직접 렌더·실행해서만 드러났다. 07-28 "queue supervisor 사망" 오진도 정정
(`grep`이 커널 `[kworker/*]`에 걸린 것이 원인).

### 변경 파일 (주요)
- 계측·프로파일링: `utils/perf_benchmark.py`, `utils/perf_profiler.py`, `analysis/flamegraph.py`
- 학습 배선·후보: `pretrain.py`, `config/cfg_pretrain.yaml`, `models/sparse_embedding.py`, `scripts/sigma_enqueue.sh`
- 테스트 6종 (378 passed)
- 문서: action-plan(비준 원장 `PERF-DEV-00..19`), resource-plan, m3-findings, perf002-plan, **p1a-refuted**, `lab/audits/2026-07-29_figqa-m3-flamecharts.md`
