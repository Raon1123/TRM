# 2026-07-29 — 랩탑↔서버 harness 동기화 라운드 3

전날(07-28) 왕복의 연장. 랩탑 `LAPTOP_REPLY2_2026-07-29.md`(6,934 B) 수신 → 서버 회신 `SERVER_ACK_2026-07-28.md` 추가분 5 (23,990 → 33,437 B) 업로드.

## 확정된 것

| 미해결 항목 | 결말 |
|---|---|
| `Tags System` 정본 | `90_Claude/80_Wiki/_tag-registry.md` (14.9 KiB, `build_vocabulary.py` 생성 · `tag_guard.py` 강제). 랩탑 답 + `paper-analysis.md.off:16` 독립 확증 |
| `4 pillars` 정본 | 그 **이름의** 정본은 없음(랩탑 전역 grep 0). 이름 삭제, 기준은 `14_LiteratureReview/CLAUDE.md` §Research Relevance Framework 4 domains 로 재지향 |
| `/mnt/ayp/vault-primary/README.md` | 고아 확정 — 랩탑이 소스·iCloud·GDrive 3중 부재 독립 관측. 삭제는 사람 판단으로 남김 |
| `attention-literature-map` 중복 실행 | 완료 취급 + NAS 산출물 읽기. 랩탑 동의. 비대칭을 **파일 주석에 고정**(양쪽) |

### `4 pillars` — 랩탑 권고(제거)와 갈린 지점

랩탑은 "추정으로 배선하지 마라, 조항 제거"를 권고했다. 서버는 **재지향**을 택했고 근거를 회신에 전부 냈다:

1. 새 의존이 안 생긴다 — paper-scout 는 이미 같은 파일을 Source Priority 로 읽는다(`.toml:24`). 랩탑 위험 모델(dangling path)이 물리지 않는다.
2. 동일성이 **파일 내부에서** 닫힌다 — (i) 축자 일치 `연결 없으면 명시. 강제 연결 금지.` ≡ `If a paper connects to none, exclude it. Never force a connection.` (ii) paper-scout 자체 Step 2 키워드가 4영역을 빈칸 없이 인스턴스화.
3. 제거하면 relevance framework 가 통째로 사라져 Tiers 의 `Exclude` 가 자기 참조가 된다.

**미결**: 최종 결정을 한쪽으로 통일해야 한다(회신 요청 1). 갈린 채 두면 두 사본이 조용히 분기 — 이 왕복이 막으려던 실패 유형 그 자체.

## 잡은 결함

- **`.claude/agents/paper-scout.md` 를 이전 라운드에서 통째로 놓쳤다.** `.codex/*.toml` 의 `[UNRESOLVED]` 마커만 쫓다가, 마커 없이 **자신 있게 틀린 경로**를 든 자매 파일을 못 봤다. 상태는 `.md` 쪽이 더 나빴다.
  - `:19` `90_Claude/CLAUDE.md "Skill Conventions"` → 그 파일에 없음(grep exit 1). 실제는 `60_Tools/CLAUDE.md:36`.
  - `:30` Source Priority, `:76` Tags System → 같은 잘못된 파일.
  - 교훈: **마커가 시선을 끌어 마커 없는 같은 결함을 가린다.** 마커 유무가 아니라 참조 유형으로 sweep 해야 한다.
- **서버 `attention-literature-map.js` 주석이 코드와 불일치**였다. 랩탑 v2 의 실측 문단이 그대로인데 상수만 서버 값으로 바꿔 놨다 — 상수를 고치면서 그 상수를 설명하는 주석을 안 읽었다.
- **stale precondition 양쪽**: "미러가 아직 안 채워졌다는 뜻(README `상태` 절 참조)" — 미러는 채워졌고(26,871) README 는 고아다. 설명절만 제거, 금지 규칙은 유지.

## 편집·검증

15 치환(`paper-scout.md` 8 + `.toml` 7), 전부 사전 `count(old)==1` 단언. 잔여 `pillar`/`UNRESOLVED`/`Skill Conventions`/`Tags System` = 0, 참조 경로 2/2 실존, 고아 README 참조 0, JS 코드 라인 153→153(주석만).
백업 `~/.claude/_harness-backup-20260729-pillars/`.

## 상시 감시

`scripts/watch_harness_inbound.sh` — `gdrive:harness-inbound` **상위 재귀** 10분 폴링, 신규/변경만 이벤트. `SERVER_ACK*` 제외(자기 유발 방지), `_MANIFEST-*` 유지(도착 신호).
좁게(`2026-07-28/`) 걸었다가 넓혔다 — 날짜 폴더 재사용은 조율 약속이지 보장이 아니고, 어긋나면 **침묵으로** 실패한다.

## 전역 sweep — 위 교훈을 쓰고 실행 안 한 걸 잡아서 실행

추가분 5 에 "마커 유무가 아니라 참조 유형으로 sweep 해야 한다"고 써 놓고, 손에 든 두 파일만 검증하고 끝낼 뻔했다. 검색 뿌리도 `~/.claude/{agents,workflows}`+`~/.codex/agents` 뿐이라 `~/.claude/commands/` 와 **`trm/.claude/`(유일한 내구 계층)** 를 안 봤다.

| 패턴 | 결과 |
|---|---|
| `90_Claude/AGENTS.md` | 0건 |
| `Skill Conventions` | 2건 — 둘 다 `literature-landscape` |
| 고아 `README.md` | 9건 / 6파일 |

`trm/.claude/` 는 3패턴 전부 0건. 내구 계층은 깨끗.

### ★ `.md`/`.toml` 쌍 불일치가 **두 번째** 재현

`literature-landscape.toml:44` = 맞음(07-28 수정), `literature-landscape.md:35` = 틀림(미수정). 07-28 의 "9곳 수정"이 쌍의 한쪽만 손댄 것이었다. paper-scout 에서 같은 걸 보고도 일반화 안 했다 — **부주의가 아니라 절차 결함**. `.md`/`.toml` 은 항상 쌍으로 검증.

덤: 두 파일 다 `relevance framework · source priority · tags` 를 `60_Tools/CLAUDE.md` 한 곳에 뭉쳤는데 실측 목차엔 셋 다 없다. `.toml` 은 **파일 이름만 맞고 내용 배정은 똑같이 틀려** 있었다. 셋으로 분할.

### 고아 README 9건 — 두 종류

- **precondition 설명절 7건** → 삭제. 능동적 오도였다: 이제 경로 부재는 "미러 미충전"이 아니라 삭제·개명·NAS 미마운트인데 문장이 틀린 진단으로 유도한다.
- **설계문서 포인터 2건** (`_template.md:14`, `hooks/config.py:55`) → `trm/lab/vault-primary/README.md` 정본으로 재지향. `_template.md` 는 신규 에이전트가 전부 상속하는 전파 뿌리. `config.py` 는 포인터가 **자기가 보호하는 미러 안쪽**을 가리키고 있던 게 본질이라 부정형 주석도 남김.

### 검증
`tomllib.load(paper-scout.toml)` OK — 7회 치환이 백틱·`§`·`—`·아포스트로피를 넣었고, 잔여 문자열 grep 은 파싱 실패를 못 잡는다(침묵형 고장). 총 21 치환, 전부 `count(old)==1` 단언. 백업 8파일.

### 모니터 결함 2건
1. rclone 지속 실패가 **침묵**으로 보임 → 연속 3회마다 `WATCH-ERROR`, 복구 시 `WATCH-RECOVERED`, 최초 스냅샷 공백이면 exit 1.
2. `sort -k3`(경로) vs `comm`(전체 줄) — 정렬 키 불일치로 크기 변경 시 오탐·누락 → `LC_ALL=C sort`.

### ★ 휘발 위험 (회신 요청 4)
오늘 고친 `~/.claude/` 밑 5파일은 다음 sync 에 **전부 되돌아간다**. 랩탑 §5 항목 3 왕복은 tex 에이전트 4개만 덮어 paper-scout·literature-landscape·link-weaver·_template 이 빠져 있다. 랩탑이 전면 동의해도 sync 가 서버를 되돌리는 구조. `hooks/config.py` 만 내구(staged).

## 대기

- 사용자: 랩탑 `settings.json` 3건(권한 분류기가 에이전트를 막음) · `/mnt/ayp/vault-primary/README.md` 삭제 여부
- 랩탑: `4 pillars` 최종 · `daily-research-digest` 경로 추가 여부 · 라우팅 표 구멍(`_tag-registry.md` 누락) 승인 요청 주체 · Path resolution 표 4파일 배달

## 라운드 4 — `LAPTOP_REPLY3` + `agents-fixed/` 14파일 (07:30 도착, 모니터가 잡음)

랩탑이 §9 에서 **"이 14개는 diff 용이지 설치본이 아니다"** 라고 못 박았다 — 내가 추가분 2 에서 얻은 교훈("위험 모델이 없다는 건 검증을 건너뛸 이유가 아니라 위험을 모른다는 뜻")을 반대 방향으로 적용한 것. 덮지 않고 대조만 했다.

### ★ 내가 만든 결함 하나 — claim-registry 오배선

07-28 에 PENDING 을 해제하며 `C1–C6` 을 `efficient-precision-efficient-depth/meta/claim-registry.yaml` 로 배선했다. 그 파일 코드계는 **`D1–D5 / P1–P4 / H1–H3`** 이고 `C[1-6]` 등장 **0회**(사본 2곳 다 확인, vault 전역에 C-계열 레지스트리 없음).

**해제 근거가 "파일이 실재한다"였지 "코드계가 맞는다"가 아니었다 — 실재 확인을 내용 확인으로 착각했다.** 이 종류가 부재 경로보다 나쁘다: 경로가 열리므로 precondition 도 존재 검사도 전부 통과하면서 내용만 딴 것이다. 마커로 되돌리되 "경로가 실재하므로 다시 배선할 유인이 크다"는 경고를 마커 안에 넣었다.

### ★ `4 pillars` — 4번째 증인, 이제 명문 근거

`commands/` 계층을 `4 pillars` 패턴으로 안 훑고 있었다(라운드 3 sweep 은 commands 에 3패턴만 돌림). 돌렸더니:

> `.claude/commands/challenge.md.off:27` — **Research Relevance Framework (4 pillars, H1-H4)**

동격 직접 진술. 축자 일치·키워드 정합·랩탑의 `paper-briefer` 확증은 전부 정황이었는데 이건 등식이다. 셋째 증인은 `researcher-profile.md.off:221`("Research Relevance Framework 4개 영역"). 랩탑이 R10.1 제거 권고를 철회했다.
덤: **`H1-H4`** 라는 짝이 여기서 처음 나왔다 — 양쪽 다 `4 pillars` 만 쫓고 있었다. 정본 위치 미상.

### 랩탑이 못 잡은 2건 (경로 집합 ↔ NAS 실물 대조)

- **`80_Wiki/researchers/` 부재** — 실물은 `30_people/`(안에 `90_profiles/`). `researcher-profile` 은 **랩탑에서 활성**이고 `output_dir` 포함 10곳이 죽은 경로. 같은 파일이 본문에서는 `30_people/` 을 쓴다 — 개명에 본문만 따라가고 프런트매터·템플릿이 안 따라간 형태. 배선하지 않고 라이브 소스 확인 요청.
- **`10_Analysis/landscape/` 부재** — `literature-landscape` 가 5곳에서 쓰는 출력 루트. precondition 이 정확히 작동해 **서버에서 이 에이전트는 기능적으로 죽어 있다.** 더 나쁜 건 `hooks/config.py:60` — `SCOPED_WRITE_ROOTS` 의 vault 쓰기 예외가 이 경로 하나뿐인데 없는 디렉터리다. **유일한 허용구가 허공을 가리킨다.** 서버가 만들어도 다음 pull 에 사라지므로 소스에서 만들어야 한다.

### §5a `.off` 범위 판정 — 접수하고 서버도 정리

`.off` 는 **머신 로컬 상태지 파일 속성이 아니다.** R8 에서 "비활성이라 범위 밖"으로 자른 4개가 랩탑에서는 전부 활성이었다. 서버에서 무해한 게 그쪽에서는 실동작 결함.
서버 `.off` 4개 + digest 양 계층 정리(9 치환, `.off` 접미사 유지).
**서버 `.codex/agents/` 는 7개 중 3개뿐** — 나머지 4개 `.toml` 이 아예 없다(랩탑은 23개). 설치 안 함: Claude 쪽이 의도적으로 `.off` 인데 codex 만 활성으로 넣으면 비활성화 우회다. §5b 비대칭의 서버 측 실측이고 **방향이 반대**다.
→ 제안: 범위 판정에서 `.off`/부재를 근거로 쓰지 않는다. 판정은 참조 유형으로만, 활성 여부는 *우선순위*에만.

### §5e 루트 `CLAUDE.md` 도달 불가 — 서버 의견 (ii)

(i) 미러 scope 편입은 이번 라운드에서 **이미 두 번 실패한 구조를 세 번째로** 만든다: `attention-literature-map` 의 OUT, `10_Analysis/landscape/` 의 쓰기, 루트 `CLAUDE.md` 의 읽기 — 전부 "서버가 vault 트리 안의 것에 의존하는데 서버는 그 트리를 못 쓴다". (i) 은 읽기를 뚫지만 의존 방향을 남기고, (ii) 는 의존을 없앤다. 결정은 사용자.

### 검증
`tomllib` 8/8 OK. 누적 33 치환 전부 `count(old)==1` 단언. 백업 14파일. 랩탑 참조 경로 9개 중 7 실존/2 부재.
잔여 `4 pillars` 는 `challenge.md` 3사본의 **의도적 금지 용어**뿐 — 이름을 바꾸면 금지 대상이 안 잡혀서 안 건드렸다(용어 분기, 회신 요청 3).

`SERVER_ACK` 41,280 → **53,575 B** 업로드.

## Handoff `[PFO]` @ 07:25

> **TL;DR**: PERF-001 계측 하네스 + flame chart 도구를 PR #1로 merge했다. py-spy로 `unique()`가
> wall time의 37.6%임을 확인하고 후보 P1-A(sparse embedding `unique` 제거)를 구현했으나,
> **동등(bit-exact)하지만 빠르지 않아 기각**했다. 원인은 스텝이 GPU-bound라 sync 하나를 제거하면
> 대기가 다음 sync로 옮겨갈 뿐이라는 것 — per-update sync 제거 후보 한 부류가 통째로 정리됐다.
> 다음 목표는 host sync가 아니라 실제 GPU 작업량(metrics `torch.stack`, CUDA의 13.4%)이다.

**Status**: ready-for-resume · **Next**: `Read lab/reports/2026-07-29_perf002-p1a-refuted.md §5 → metrics 로깅 주기 후보를 사전등록(관측가능성 변경)한 뒤 A/B 측정`

→ 전체 handoff 문서: `lab/handoffs/2026-07-29_perf-flamechart-p1a-refuted.md`

## MCP 매뉴얼 세션 (오후)

사용자 지시: 계획 로드 → 실질 구현 codex 경유 → 상태·연결 매뉴얼 → 의사결정 문서화.

- **사실 확인**: v1+T10b는 07-22 완료 상태 그대로 — 스위트 재실행 **350 passed**. 남은 것 = 라이브 배포(사용자 행위) + open Q3/Q5/Q6.
- **산출**: `lab/mcp/MANUAL.md`(초안 codex-work) → codex-verify 감사(**FAIL: WRONG 3 + MISLEADING 2**) → 수정 후 `trm-mcp/docs/MANUAL.md`로 승격. 의사결정 로그 `lab/mcp/audits/2026-07-29_manual-and-codex-pipeline.md`(D-s1~s5 + 기각 대안).
- **감사가 잡은 실기능 결함**: ① "성공 시 200" curl은 실제 403(Origin 부재 시에도 403)→406(SSE Accept) — 성공 프로브는 헤더 3개 필요 ② **SSH 터널 경유 시 whitelist에 넣을 IP는 워크스테이션이 아니라 터널 출구 소스 IP**(proxy_headers=False) ③ T2 목록 누락(pr_recompute·일반 폴백·.netrc basename 별도 검사). `claude mcp add` 문법도 초안이 비실재 플래그 사용 — `--help` 실측으로 교체.
- **stale 문서 수정**: OPS.md §5.3 provenance 행 — "T07 pending·미배선" 주장은 구문; 실측 label_for 4모듈 배선(T07 완료 07-22)으로 교체.

## figure-pipeline / fig1 provenance 세션 [FGP] (오후)

사용자 지시: 56셀 복구 → NAS 지시 이행 확인 → 현황·handoff 정리.

- **fig1 48/56 → 56/56 복구.** 07-28에 8개 mlp 셀이 빠진 원인은 큐 kill+retry로 한 run_name 아래
  wandb 디렉토리가 2개 생겨 quarantine-unless-unique가 셀을 통째로 떨어뜨린 것. 신규 스냅샷
  `ds-sigma-k-new-20260729-7c3f44b48859547c`, QA **16/16 pass**, `rtk uv run pytest tests/` **651 passed**.
- **지시받은 규칙은 측정으로 반증.** "`config.yaml`을 가진 dir을 고른다"는 fig1 8/8이지만 ablation은
  **4/8만** 판별한다(`abl_{H6,L3,halt8,halt16}_k3_s1`은 해당 dir이 2개). 대신 **M23 — 사전등록 완주
  기준**(`terminal_step=244100 ∧ eval_rows=50`, 완주 후보가 정확히 1개일 때만 선택, 2개·0개면 판정
  거부)으로 구현. 훈련 config에서 나오는 값이라 메트릭과 무관하게 먼저 고정된다(post-hoc 아님).
  중복 16그룹 전부 완주 후보 1개, quarantine 8건 전부 `duplicate-retry-superseded`.
- **`CONFIG_DIGEST_FIELDS`는 의도적 비변경** — digest가 `args`를 직접 인덱싱해 필드 추가 시 기존
  receipt 전부의 재계산이 깨진다(인도된 보고서가 인용하는 것 포함). in-code KNOWN GAP + 별도 항목.
- **새 결과 2건**: ① `tf_noz_iter`의 k∈{8,10} 비단조 회복에 **mlp 대응물이 없다**(mlp는 k=5..8,10 전부
  H) — cycle type은 데이터의 성질이라 양쪽에 나타나야 하므로 **C4의 gcd 판독 거부에 직접 논거**가 된다.
  ② EXP-007의 tf/mlp 상충은 k=9 이상치가 아니라 **k=8·10에서도 재현되는 일반 패턴** — EXP-007 §2
  tie-break를 k=11 뒤에 고치면 사후 조정이 되므로 **k=11 실행 전 문서화 필요**(유일한 시간 제약 항목).
- **PREVIEW 잔존 사유는 이제 하나뿐.** `queue_preflight` 술어는 `complete = (jobs+processing==0) and
  (failed==0)`이고 현재 큐는 `0/0/56/0` → coverage 게이트 **열림**(07:35 당시엔 `processing:1`이 유일한
  실패 사유였다). 남은 것은 `source_commit_dirty` 하나인데, 트리를 더럽히는 것은 **다른 트랙의 진행 중
  작업**(cleanr1 도구·`M pretrain.py` 등)이라 이 세션이 닫을 수 없다. dirty가 남는 한 재실행은 여전히
  PREVIEW이면서 인도된 보고서의 인용 스냅샷만 supersede하므로 **재실행하지 않았다**.
- **NAS 3호스트 중 1/3 이행**: `10.0.12.93` 완료(rsync 3,285 files / 10.96 GB). `unistml7`은 **코호트
  미기동** — GPU 4장 전부 유휴 + `results-return` 비어 있음 + md5 보고 미반환의 독립 3신호로 확정(heartbeat
  자체는 246 B에 꼬리가 `}\n;"\n}\n`로 **찢어져 유효 JSON이 아니라** 신뢰 근거로 못 씀 → STEP 6 원자성
  미적용). `imil0`는 NVML 580.173 불일치 지속(관리자 조치 필요). matplotlib 미문서화 의존성은 **해소**
  확인(`pyproject.toml:30` + uv.lock).
- **신규 발견**: 라이브 코호트 `cleanr1_*`(12:43, 18 프로세스)가 **`+project_name=Sigma_k_new`** — fig1과
  같은 프로젝트다. 큐 밖에서 돌아 현재 게이트는 무사하고 fig1 acquisition도 정확한 이름 `include_only`로
  fail-closed지만, 빡빡한 expected-set 없는 향후 acquisition은 이제 `cleanr1_*`를 끌어온다.

**Status**: ready-for-resume · **Next**: `Read lab/audits/2026-07-29_fig1-56cell-recovery.md §7 → 보고서 §6.1·§7 개정을 k=11 arm 전에 착수`

→ 전체 handoff 문서: `lab/handoffs/2026-07-29_fig1-56cell-recovery-provenance-1248.md`

### 라이브 배포 (사용자 지시 후속)

- bind **10.20.22.130:8765** — 사용자 발화 "local=10.20.23.74"는 실측(`hostname -I`)상 비소유 주소라 **소비자로 재해석**해 whitelist 배치(D-s6, 사용자 확인 필요). whitelist = 10.20.23.74(로컬)+서버4(10.0.12.93/94, 10.20.22.56/73)+자기자신(검증용, D-s7). token/env는 `.env.production`(600) 단일 위치.
- **라이브 첫 검증이 4중 감사가 못 본 결함 적발**: FastMCP의 SDK transport-security 기본값이 loopback 한정 → 내부망 bind 전 요청 **421**. in-process 테스트는 Host 헤더를 안 보내서 통과했었다. 명시적 allowlist 배선+회귀 3종, **353 passed**, initialize POST **200+session-id** 실측. 교훈: **살아있는 소켓 검증은 in-process 스위트가 대체 못 한다.**
- 방화벽 ufw는 sudo 부재로 미적용 — 사용자 조치 대기(MANUAL §1.1).
